implementations function main
begin
    set user = 17
    if user <= 18
    then
        display "User is 18 or younger"
    else if user <= 35
    then
        display "User is young adult" 
    else
        display "User is old..."
    endif
endfun